只引用当前已加载到 Media Pool 的图片编号，绝不引用未加载的编号。当前只有 `@P1` 时，`@P1` 是红色航线控制图：读取红线的起点、路线、转弯与终点。不要在当前 Design 或 H3 Prompt 中写入任何未加载的 Picture 编号。

若缺少开场或结尾画面，先使用 Z-Image 参考 `@P1` 创建“开场环境参考图”和“结尾目标参考图”的媒体请求；补图必须保持同一城市、建筑、天气、光线与地标连续性。等待补图生成并实际加载到 Media Pool 后，再重新执行 Design，并只使用届时真实存在的编号。

制作一段连续的 MiniMax H3 航拍视频：无人机沿 `@P1` 设定的红色路线飞行，完成一次受控的 360 度环绕拍摄或偏航（yaw）旋转。红线默认仅作为路径控制，不出现在最终视频；只有用户明确要求时才显示红色航线动效。飞行必须平滑、稳定水平线、具有自然航拍视差与符合物理规律的加减速，并避免与可见建筑、道路、电线、树木、车辆或人物发生碰撞；禁止瞬移、随机旋转、镜头抖动、穿模或突然切换场景。
