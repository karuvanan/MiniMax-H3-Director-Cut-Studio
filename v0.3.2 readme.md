# MiniMax H3 Director Cut Studio v0.3.2 / v0.3.2-alpha.2

本文件集中记录 v0.3.2 的重点功能、长片制作流程、Workspace 结构与正式可靠性基线。通用安装、模型准备及日常操作请返回 [`README.md`](README.md)。

## v0.3.2 重点功能

### v0.3.2-alpha.2 · Reusable Special Skill Design Templates

这一版把 Special Skill 从“风格说明”升级成可以反复复用的影片生产入口。用户不需要每次重新写完整镜头、对白、动作预算、声音、素材映射和 Segment 连续性要求；选择同一个 Skill，再替换人物、产品、图片、场景或故事主题，Design 就会按照相同的成熟规则重新规划另一支视频。

#### 自动填入 Design Requirement

- 每个 Special Skill 现在都可以在自己的文件夹内提供独立 `DESIGN_REQUIREMENT.txt`。
- 在主页选择 Special Skill 后点击 `DESIGN`，如果 Design Requirement 仍为空，Studio 会自动填入该 Skill 的标准制作范例。
- 模板是普通可编辑文字。用户可以直接改地点、人物、时长、画幅、对白、产品、素材和结尾，再交给 Design 分析。
- 已有的用户输入永远优先；重新打开 Design 时不会用模板覆盖已经存在的 Requirement。
- 没有模板文件的第三方或旧 Special Skill 保持原有行为，不会影响载入、保存或旧 Project。

#### 同一个 Skill，持续转换不同素材与场景

例如选择 `wuxia-blade-film` 后，第一次可以使用唐代庭院、长枪将军与双短剑刺客；下一次只需把 Requirement 改成雨夜码头、断刀客与双刀敌人，并换入新的 Media Pool 图片。Skill 会继续执行相同的动作预算、兵器几何、人物身份、伤势、空间路线、15秒边界和 Final Hold 规则，但不会复制上一支影片的角色、素材或场景。

这套机制的重点是：

- **规则可以复用，内容不必重复**：Skill 保存制作方法，Requirement 决定本次故事。
- **素材可以替换**：同一 Skill 可以使用不同照片、人物、产品、环境、Audio 或 Video reference。
- **复杂细节自动继承**：镜头职责、动作优先级、对白图层、环境声、Reference Mapping、Incoming／Outgoing State 和 Segment 限额由 Skill 与 Design 共同整理。
- **仍然完全可编辑**：自动模板不是锁定 Preset；Apply 前可自由删除、扩写或改写。
- **不虚构素材编号**：模板没有明确写出 `@P/@V/@A` 时，Design 不得自行假设不存在的 `@P1`、`@V1` 或 `@A1`。需要真实素材的 Skill 只使用当前 Media Pool 已加载并勾选的项目。

#### 内置12套起始模板

| Special Skill | 自动模板用途 |
| --- | --- |
| `3d-animation-short-generator` | 45秒风格化3D动画故事、角色与环境连续性 |
| `brand-promo-video-generator` | 15秒品牌／产品宣传、真实资产与可验证卖点 |
| `co-op-game-intro-generator` | 双人合作游戏菜单、玩家身份与UI文字 |
| `dark-rescue-h3` | 第一人称暗场警察／消防救援、现场光影与声音 |
| `handdrawn-live-video-generator` | 15秒实拍空间与发光手绘实体连续变形 |
| `long-form-h3-director` | 120秒长片 Timeline、批准范围与原生 Segment 合约 |
| `minimalist-product-ad-generator` | 10秒极简产品发布、真实产品颜色与单行英文文案 |
| `music-video-subtitle-generator` | 30秒 MV、Master Audio、歌词和节拍硬切 |
| `paper-collage-explainer-generator` | 编辑式纸拼贴视觉隐喻与纸张现场音效 |
| `papercraft-stop-motion-explainer` | 纸艺定格知识解释、立体景深与普通话旁白 |
| `short-drama-h3-director` | 60秒竖屏短剧、因果冲突、逐字对白与集尾钩子 |
| `wuxia-blade-film` | 45秒写实高速武侠、武器账本与动作预算 |

每个模板位于：

```text
skill special/<skill-name>/DESIGN_REQUIREMENT.txt
```

#### Special Skill Creator

`SPECIAL SKILL CREATOR` 新增 `DESIGN REQUIREMENT TEMPLATE` 编辑区。建立或修改 Special Skill 时，可以同时维护：

```text
SKILL.md                  # Studio 实际执行的英文规则
SKILL.cn.md               # 可选中文对照版
DESIGN_REQUIREMENT.txt    # 点击 Design 时自动填入的可编辑起始要求
```

点击 `SAVE + APPLY` 后，Skill 列表会立即刷新并选择刚保存的项目。为兼容内置完整说明，Creator 的 Description 上限由600字符提高至1200字符。

#### Dark Rescue H3

新增 `dark-rescue-h3`，采用 `Default H3 Prompt Writing + Special` 绑定。它把五套经过实际创作的暗场救援经验整理为可复用规则，涵盖废弃校舍、曼谷耀华力路后巷、吉隆坡茨厂街后巷、历史城寨重建空间，以及受华尔街金融区启发的虚构损坏高楼。

该 Skill 默认建立45秒、九个约5秒 Shot、两个稳定说话身份、普通话对白／克制旁白、第一人称路线、物理障碍、现场环境声和三个原生15秒动作阶段。光影只使用已经验证的低调照明、手电光束、体积散射、短促故障闪烁、背光剪影、机械阴影、红色警示灯、湿面高光和写实工业残损；每个效果都必须有可见物理来源。

#### 使用步骤

1. 在主页保留 Default Skill 为 `H3 Prompt Writing`。
2. 在 `+ Special` 选择目标 Skill。
3. 点击 `DESIGN`，检查自动填入的 Requirement。
4. 替换本次影片的人物、素材、场景、时长、对白或主题；Media Pool 有指定素材时才写对应 `@P/@V/@A`。
5. 让 Design 生成并验证 Director Design JSON。
6. Apply 到 Timeline 后，仍可使用 Storyboard、Type Tool、Track、Smart Cut 和局部重算继续修改。

#### 验证与兼容性

- 12个内置 Special Skill 均具备非空、可编辑且可被 Studio Profile Loader 读取的模板。
- 自动测试覆盖模板存储、Creator 载入／删除、Default + Special 绑定、Design 自动填入及“不覆盖已有文字”。
- 38项相关 Skill Store、Profile 与 Design UI 测试通过。
- Project Format 保持 `21`；Workspace、Timeline、Reference Mapping、Segment 编译、Render Policy、旧 Project 和生成媒体均未改变。

### v0.3.2-alpha.1 · MiniMax H3 Native Production Sound

- **原生现场对白方向**：每个 Shot 自动生成可编辑的 `Native Audio Direction`，把可见空间、人物到镜头距离、画面方位、说话状态、持续环境底噪、同步 Foley 与 diegetic source 规则写入实际提交给 `MiniMaxH3ReferenceToVideo` 的六段式 Prompt，不是只在 UI 显示。
- **无后期改写链**：此模式不会新增 TTS 替换、语音分离、FFmpeg 混响、EQ、卷积混响、Echo 或后期混音。H3 原始音画输出保持不变；已有 Vox／Qwen／Edge 模式仍只有在用户明确选择时才工作。
- **声学连续性**：同一空间跨 Shot／Segment 延续环境床与说话距离；远景切近景只改变直接声比例；室内外切换明确结束旧空间并建立新空间。上一段最后 24 帧仍是无声视觉运动参考，绝不把旧音频送入下一段。
- **参考音频用途锁定**：明确标记为现场环境的 Audio／Video 只提供空间声学、环境底噪、说话距离和现场质感，不复制原台词或声线，也不覆盖 Timeline Speaker。
- **Native Audio QC**：生成后复用现有 VAD／Whisper 做只读检查，报告目标台词、未授权额外对白和环境声缺失风险。QC 只显示结果，绝不修复、重混或重写音轨。
- **可保护的 Shot 字段**：Shot 编辑器新增 `Native Audio Direction`、`Environment Continuity`、`Audio Reference Intent`、`Native Audio QC status`。自动生成是默认行为；任何人工改写都会被标记为 override，后续刷新不会覆盖。
- **验收结果**：完整 `411` 项测试、十一项 Standard Pipeline Release Gate，以及独立的 `41` 项 Workflow／Segment／Storyboard Mapping 回归全部通过。

- **Long Production Reliability 正式验收**：Smart Render Job 与 Manifest 固定写入当前 Workspace 的 `project/render_jobs/`，不同 Project／Studio instance 不会复用同名任务。ComfyUI 上传名同时包含逻辑 P/V/A 与本地 source fingerprint，即使两个项目都有不同内容的 `P1/shared.png` 也不会覆盖。每个 Segment 最多尝试 3 次；CUDA OOM 会分类记录、释放 ComfyUI 内存后重试，失败原因与 retry budget 写回 Manifest，不会偷偷降低画质。
- **崩溃恢复不依赖 UI 内存**：Worker 每次完成或失败 Segment 都原子更新 Manifest。重新打开 Project 会比较所有 canonical／worker checkpoints，优先采用拥有最多真实可用 Segment Takes 的版本；较新的失败空清单不会覆盖较旧但更完整的进度。
- **Project Storage 工作台**：主页 `STORAGE` 显示 logical／physical bytes、Hard Link 节省、durable Segment Takes、Master、media、cache 与重复实体文件。`SAFE CLEANUP` 先显示 dry-run 清单并二次确认；Approved Takes、Master、Media Pool、Project，以及中断任务仍需要的恢复缓存永远受保护。
- **可验证 Portable Archive**：`ARCHIVE PROJECT` 建立 `.h3project.zip`，默认排除可重建 cache／proxy／logs，合并外部 workflow 与 media，并用 `archive_manifest.json` 保存每个文件的 SHA-256。活动或尚未持久化的中断任务存在时会明确阻止 Archive，避免制作无法恢复的不完整备份。
- **长片架构验收**：自动测试覆盖“已批准 45 秒扩展至 120 秒”、8 个 Segment 选择性拼接、只替换中段且外段 Hash 不变、完整 120 秒 H.264/AAC Master 与 Archive；另以 900 Shots／5400 秒执行无 H3、无网络、峰值内存受限的 90 分钟元数据压力测试。
- **十一项 Standard Pipeline Release Gate**：在原有 Mapping、Timeline 同步、Track 类型、15 秒边界、跨电脑素材、`unique_media`、Take reload、Apply transaction、Virtual Pool 与对白扩时检查之后，新增“中断恢复缓存／Safe Cleanup／Portable Archive”强制 Gate。
- **Storyboard／Smart Cut Segment Mapping 对齐**：移动、删除、增加或缩短 Shot 时，Picture／Video／Audio Clip、Text Layer、Source In/Out 与 Shot ownership 会一起映射到新时间；旧 Master 立即失效，但可复用 Segment checkpoint 不再被整批清空。系统同时标记改变前与改变后的时间范围，防止已批准 Segment 因移动后位置改变而被错误复用。
- **精确局部失效**：结构编辑内部的 Prompt UI 刷新不再误把整条 Timeline 标为 dirty。实际未受影响、Fingerprint 相同的前段 Segment 保持 cached；发生缩时、位移或跨 Segment 的 Shot 以及其后受 Ripple 影响的范围才进入重算。

- **Monitor Proxy 原子生成**：高分辨率影片的播放代理先写入独立 `.building.mp4`，通过 FFprobe 检查视频轨、时长及完整性后，才会原子发布为正式缓存。关闭 Studio 不会再留下一个看似存在、实际缺少 `moov` 索引的 MP4 给下次载入。
- **损坏代理自动恢复**：重新打开 Project 时若发现旧代理损坏、被截断或时长不完整，Studio 会自动删除该可重建缓存，并从完整的 `generated_output.mp4`／`generated_preview.mp4` 后台重建；不会修改 Master、Segment Takes、Timeline 或 Mapping。

- **Generated Output 自动恢复**：即使 Project JSON 的 `generated_output` 或 Smart Render 字段为空，只要 Workspace 中仍有 `generated_output.mp4`、`generated_preview.mp4`、`render_manifest.json` 或 Segment Takes，重新打开就会恢复 Program Monitor 播放、Export 与 Render 状态，不再错误显示 `No generated output`。
- **跨电脑／改名 Workspace Manifest 重定位**：Manifest 仍指向旧电脑或复制前文件夹时，会自动改用当前 Workspace 的 Master 与 `segments/*/takes/`。恢复不会复制、移动或重算 MP4；Project 会标记待保存，点击一次 `SAVE PROJECT` 即可持久化 canonical 路径。

- **Picture 本地路径即时缩略图**：只要 P 素材的 `local_path` 是有效图片，Media Pool 会立即直接显示原图，不再等待 Media Preparation。后台仍可随后替换成优化 Preview 或自动去背景版本，但不会再短暂或永久显示 `DROP MEDIA`。
- **覆盖所有图片进入路径**：旧 Project 跨电脑路径重定位、Design 生成图片、手动替换、Z-Image 重生及自动透明背景衍生图都会同步刷新卡片与内存 Preview Cache；不改变 P 编号、Reference Mapping、Timeline 或生成结果。

- **Work Area End 可自由延长 Timeline**：新项目仍默认 `12s`，但 End 不再被当前 Timeline 终点锁死。直接输入或点击增加至 `45s`、`120s` 等数值，Studio 会同步扩展 Timeline、工作流 duration、播放范围及保存工程时长；之后把 End 调小只会缩短本次生成范围，不会裁掉既有内容。
- **长时间输入与安全校验**：手动 Timeline 最长可设为 6 小时，覆盖未来 90 分钟项目；End 若早于 Start，会自动调整到 Start 后一个 `0.5s` 网格。滑鼠停在 End 上可查看完整功能说明。

- **旁白／对白 Segment 归属修复**：尾音保护不再把安全切点推过下一句的起点。8.5 秒、22 秒、41 秒开始的文字层会留在自己的 Segment，不会提前发声后在原定时间消失。
- **S1／S2 可见人物硬绑定**：原生 H3 Prompt 明确固定 `S1 = 女性`、`S2 = 男性`，并绑定到该 Segment 动态映射后的实际 Picture Tag。只有当前讲话者可以动嘴；聆听者闭口，Voice-over 不驱动画面人物口型，防止两人身份和发言画面对调。
- **Run 前实时对白修复**：Preview／Run 会再次检查当前可编辑 Timeline；普通话误标 English、过密对白、后续 Ripple、Work Area 以及末句 1.5 秒 Room Tone／Reverb Tail 都会在提交前修复，不要求重新进入 Design。
- **Smart Render policy 15**：旧 policy 14 中已生成的错误对白 Segment 会自动失效。`那个替他说“不”的_AI_4` 会修复为 72 秒及十个对白安全窗口；必须重新执行 Preview／Run 才会得到修复后的声音和人物口型。

- **旧 Project Text Layer 载入修复**：自动对白延时产生的 `speech_timing_auto_adjusted` 现在是正式可持久化字段，不再触发 `TextLayer.__init__() got an unexpected keyword argument`。Text Layer 载入器也会安全忽略未来版本的附加元数据，避免新的自动修复标记阻止旧 Project 打开。

- **70 秒末句硬切修复**：旧 Project 在载入时会按保守的普通话安全语速重新检查 Dialogue／Voice-over／Lyrics，自动 Ripple 所属 Shot、后续 Shot、Text Layer、素材和 Work Area。最后一句至少保留 1.5 秒呼吸、Room Tone 与 Reverb Tail，不再在项目终点切断最后一个字。
- **原生对白严格白名单**：每个 Segment 只允许 Timeline Text Layer 列出的台词与时间窗发声；台词空档禁止填充音节、假起句、重复句、翻译、电话杂声或额外旁白，针对 25–29 秒污染和 31–32 秒断句。
- **可见文字必须由 Track 拥有**：Shot／Global Prompt 中未建立 `on_screen_text` 图层的字幕、标题、引号文字及“text-only on screen”指令会在编译时移除。手机、显示器和界面在无 Type Track 时必须保持空白、虚焦或不可读，防止 55 秒后出现巨型乱字幕。
- **旧 Project Prompt 同步**：载入时若发生对白时长修复，Studio 会立即用修复后的 Timeline 重建可编辑 Prompt，避免下次 Run 又使用旧的 70 秒时码。当前 Smart Render policy 15 会废弃已缓存的错误 Segment。

- **对白安全的 Segment 音频边界**：Dialogue／Voice-over／Lyrics 不再被 15 秒 H3 原生边界切成两半。Smart Render 会把内部切点移到完整句子结束后的自然呼吸／混响尾音；若三个 15 秒窗口已经完全占满，则自动增加一个安全 Segment，而不是截断、漏字或在下一段重播整句。每个窗口同时收到连续 Room Tone、禁止尾秒新起音与自然衰减合约。
- **Master 接缝 De-click**：Preview／Final 拼接时对非首尾 Segment 音轨加入 40ms 微型淡入淡出，抑制 AAC 波形不连续造成的爆音与数字硬切，不改变视频帧、Segment 时长、Reference Mapping 或 Timeline 时间。
- **真实项目修复验证**：`那个替他说“不”的_AI_2` 会由 70.0 秒修复为 77.5 秒，并规划为 `0–8.5 / 8.5–22.5 / 22.5–35.5 / 35.5–45 / 45–58.5 / 58.5–66.5 / 66.5–77.5s`。当前 policy 15 会让旧缓存安全失效，重新 Preview 后才会得到新音轨。

- **Media Pool 单图 Z-Image 重生**：右键任何已具备 Prompt／Recognition 的 Picture 卡片，选择 `REGENERATE WITH Z-IMAGE`。Studio 会读取该 P 素材原始 `.request.json`，保留人物人数、身份与道具归属、场景、构图、时间范围、关联 Shot 和永久 P 编号，只生成新的视觉 Take 并原位替换。旧图不会被覆盖；新图存放在 `media/regenerated_references/P#/`，相关 Segment 自动标黄待重算，其他 Segment 与 Timeline Mapping 保持不变。

- **Reference Mapping 污染修复**：生成参考图会建立严格 Cast Ledger。全片只保留一个主身份锚点；另一位演员使用独立身份合约；人物图固定精确人数；环境／道具图禁止背景人物。机场、酒店、电脑、清晨等场景参考只在所属 Shot 时间生效，早期场景不能继续映射到末段。Smart Render 虽然只上传一次全项目素材联合集，但每个 Segment Manifest 现在只记录其 Workflow 真正连接的 Loader，方便直接审计映射。
- **对白延长与 Shot 同步**：Dialogue／Voice-over／Lyrics 因自然语速延长工程时，所属 Shot 与后续 Timeline 项目会一起 Ripple；0.00 秒至最终 duration 每一帧必须恰好属于一个 Shot。旧 Project 打开时会自动修复末段 `shot_ids=[]`、重新绑定 Text Layer，并把旧 Take 标为需要重算。
- **真正不限总数的 Virtual Media Pool**：`9 Image / 3 Video / 3 Audio` 只代表单一 H3 Segment 的实体容量，不再限制整个 Design 的 media request 总数。项目可以继续建立 P10、P11、P12、V4、A4 等逻辑素材；只要同一个 Segment 的并发引用不超过 9/3/3，就会动态换入实体 Loader。
- **多地点 Montage 拆 Shot**：旧 Project 中一个 Shot 若连续写出多个 `Cut to` 地点，会按现有参考图边界或 0.5 秒网格拆成单地点 Shot，保留每个故事 Beat，避免 H3 在同一生成镜头混合机场、酒店、影院或办公室。
- **Qwen 结构合约升级**：Design Planner 必须先建立人物／道具／地点 Ledger，逐 Shot 写明精确可见人数；两人对白只允许两名指定演员。每个 Shot 只能有一个地点和主要镜头运动；多地点必须拆 Shot；环境参考必须 time-scoped；对白扩时必须同步延长所属 Shot，禁止产生无 Shot Segment。

- **统一 Apply Preflight Report**：Design 的 Validate／Apply 不再逐个弹出错误视窗。所有结果集中显示为 `AUTO-FIX / WARNING / HARD BLOCK`；时长、Brief／Style、重复 requirement ID、Z-Image Prompt 和 Shot overlap 等可预测问题先自动修复，普通风险高亮但允许继续，只有无法建立可执行 Workspace 的问题才阻止 Apply。完整 17 类验收矩阵见 [`APPLY_ARCHITECTURE_ACCEPTANCE_CHECKLIST.md`](APPLY_ARCHITECTURE_ACCEPTANCE_CHECKLIST.md)。
- **事务式 Apply 生命周期**：点击 Apply 后 Design 页会等待真正的 Timeline／Workspace commit；提交失败、Worker 忙碌、FFmpeg／文件系统异常时保留原 JSON 和可重试界面，不会关闭窗口制造“已经 Apply”的错觉。只有 `_commit_ai_design` 成功后才自动关闭。
- **TTS 降级不毁 Design**：VoxCPM2 模型缺失或三个实体 Audio slots 已满时，精确 Dialogue／Voice-over／Lyrics Text Layers 仍会进入 Timeline，WAV 改为待处理并高亮；不会生成静音占位，也不会因为声音条件挡住全部 Shot。
- **重叠 Shot 全自动修复**：一般 overlap 使用共同 0.5 秒 Cut boundary；连两个 0.5 秒格都放不下的极短冲突会把两组核心动作与连续状态合并成一个可执行 Shot。Media／Text tracks 的合法重叠保持不变。

- **Generation Work Area 顺序自动换行**：控件严格按 Work Area 开始／结束、Aspect、Mode、Batch、Next、批准秒数、Export、IP、Test、Run、Storyboard、Story Duration、Auto Cut、Preview、Accept、Reject、Estimate 排列。第一排放满后，剩余控件依序自动常驻第二排。
- **取消 MORE 与 Hover 展开**：不再显示 `MORE ▾`，也不需要滑鼠叫出第二排。只要视窗宽度需要换行，第二排便自动显示，两排都不会出现 Qt 系统 `»` 溢出菜单。
- **紧凑秒数与按钮**：Work Area 使用 `0s`、`15s`、`0.5s` 这类动态紧凑显示，仍保留 0.5 秒 Timeline 精度；`TEST`、`PREVIEW 0.2M`、`ACCEPT 1.0M` 与固定宽度 IP 输入框减少不必要占位。
- **即时完整 Hover Help**：Work Area Start／End、Aspect、FULL／BATCH、Batch 秒数、NEXT、Export、ComfyUI 地址、Run、Storyboard、Smart Cut、Preview、Accept、Reject 与 Estimate 均显示完整功能名称、作用和结果；缩写按钮不再需要靠猜测。

- **Incremental Long Production**：主页新增紧凑的 `MODE: FULL / BATCH`、Batch 秒数、`NEXT` 与 `✓已批准/总秒数`。默认 Batch 为 30 秒；`NEXT` 只 Preview 尚未批准的新范围，`ACCEPT 1.0M` 才建立由 0 秒到当前批准点的累计 Master。已批准 Segment 直接复用，只处理新增或黄色 dirty 范围。
- **Long-form Special Skill 自动绑定**：选择 `long-form-h3-director` 会自动切换到 Incremental；选择 None 或其他 Special 恢复 Full Range。Skill 负责完整故事、Sequence／Shot 职责、Incoming／Outgoing State、24 帧上下文、逐字 `text_layers`、Segment 局部参考与唯一项目终点 Final Hold；Runtime 负责真正分批渲染，两者职责分离。
- **安全批准点**：30／60／90 秒只是批准点，不是剧情结尾。Studio 遇到跨批准点的 Dialogue／Voice-over／Lyrics 会自动把边界延长到整句结束，并优先落在附近 Shot 边界，防止为了批次截断台词或制造假 Final Hold。
- **可恢复 Batch 状态**：Project 保存批准进度、待处理范围、阶段、Preview seed 与 Preview Ready 状态。每个隐藏 Segment checkpoint 都会保存这些信息；中断后 Open Project 可继续 Preview／Accept，而不是从 0 秒重跑。

- **Smart Cut 第 1 阶段 · 确定性压缩**：主页 `SMART CUT` 会读取当前 Shot、逐字 Dialogue／Voice-over／Lyrics、核心动作、连续状态和 Timeline 素材，在固定 0.5 秒网格上计算 Safe、Balanced 或 Aggressive 压缩方案。对白、开场钩子、线索、反转、高潮和 Final Hook 默认受保护；目标不可能安全达到时会明确显示最安全时长，不会静默切断台词。
- **Smart Cut 第 2 阶段 · Dependency Graph 与局部重算**：规划器建立相邻 Shot 的连续状态、共享 P/V/A 素材、同轨关系和 15 秒原生边界风险；每个决定显示 KEEP、TRIM、MERGE 或 REMOVE、重要度、风险及原因。Apply 会同步重映射 Shot、Type/Text Layer、Transition、Marker、Audio／Video Source In/Out，并只把受影响范围标为待重算。完整计划保存为项目内的 `smart_cut_plan.json`，可 Undo/Redo，也会随 Project 保存和恢复。
- **Smart Cut 第 3 阶段 · 受约束 AI 语义辅助**：`AI SEMANTIC REFINE` 可使用 Design 中当前选择的 Online GPT 或 LM Studio 模型分析 Hook、Clue、Reveal、Climax、Final Hook 和重复 Beat。LM 只可提交有限的语义提示，不能提供 Timeline 时间、删除对白或直接改工程；所有最终秒数仍由本地确定性规划器重算。实际解析到的 LM Studio 模型会在分析后正确卸载。
- **非破坏式 Review**：Smart Cut 独立视窗先显示 Original、Edited、Target、保护警告和逐 Shot 决策；可以锁定 Shot、改动作或以 0.5 秒调整时长。Cancel 保证 Timeline 完全不变，只有 `APPLY SMART CUT` 才提交单一 Undo 操作。原来的紫色 Shot 手动 Ripple Cut 仍保留在按钮箭头菜单中。
- **完整 Auto Cut 延后至 v0.4.0**：当前 Smart Cut 是 Long-Form 阶段使用的安全压缩基础。关键人物首次出现、因果职责、跨全片重复说明、独特画面价值、可替代程度及完整 Importance 加减分属于大型故事剪辑升级，已登记到 Long-Form Roadmap 的 `v0.4.0 Story-Aware Auto Cut`，不会继续扩大本轮 v0.3.1 的验收范围。

- **Storyboard 实体拖放反馈**：滑鼠停在卡片上会浮起并出现阴影；抓起时显示带软阴影的半透明卡片；拖过目标位置时其他 Shot 会平滑让位；释放后卡片以回弹和青色边框确认落点。`←` / `→` 移动也使用相同落位效果。

- **Storyboard 左右移动按钮**：在 `SELECTED SHOT` 右侧使用 `←` / `→`，可把当前 Shot 精确移动一个位置；首张和末张会自动禁用不能使用的方向，并沿用卡片滑动动画。
- **8 种 Storyboard View**：右上角 `VIEW` 可切换 Extra large icons、Large icons、Medium icons、Small icons、List、Details、Tiles 与 Content。默认仍为三列 Large Icons；选择会写入项目，重新打开后保持不变。

- **每个 Shot 的专属 Picture**：Storyboard 不再因为 P1 覆盖整条 Timeline 而让所有卡片重复显示 P1。缩略图依次采用 Shot 明确指定的 reference、起止时间完全匹配的独立 Picture，最后才回退全局身份 P1；卡片底部显示 `FRAME P#`，可以直接检查画面来源。
- **Lift / Placeholder / Slide Drag & Drop**：拖动时会出现跟随滑鼠的卡片 Ghost，原位置留下 `DROP HERE`；滑过目标卡片时占位会实时移动，其他卡片以 150ms OutCubic 动画滑入空位。放下才确认草稿顺序，取消拖动则回到原位。

- **三列 Visual Storyboard**：Storyboard 中央区域改成参考 Premiere/电影分镜板的三列 16:9 卡片。每张卡片显示对应 Shot 的真实图片或视频缩略图、时间范围、时长、Preset、核心动作、环境反应、Speech 数量和 P/V/A 素材；没有有效画面时使用独立占位，不会借用相邻 Shot 的图片。
- **真正 Card Drag & Drop**：Shot 卡片使用 Internal Move 拖拽，具有 Drop Indicator 和网格吸附。放开滑鼠后会立即重排 Shot 编号与时间，顶部 Edited、Target、Over/Under 秒数同步更新；仍然只有点击 `APPLY TO TIMELINE` 才会改变主时间轴。

- **Advanced Storyboard Editor**：点击主页 `STORYBOARD` 会打开独立 Shot 卡片视窗。卡片直接读取 Timeline 的 Shot、核心动作、Dialogue/Voice-over 数量和 P/V/A 素材编号；支持拖拽改顺序、`+ SHOT`、`DELETE SHOT`、修改标题/核心动作及以 0.5 秒调整时长。顶部实时显示 Original、Edited、Target 和 Over/Under 秒数。
- **Apply 才回写 Timeline**：Storyboard 内的所有操作先留在草稿；`Cancel` 不改变 Timeline，`APPLY TO TIMELINE` 才一次性同步 Shot、Transition、Marker、Type/Text Layer 和素材时间。Apply 是单一 Undo/Redo 操作，并会令受影响的生成结果进入待重算状态。
- **Storyboard 依赖重映射**：移动或缩放 Shot 时，所属对白、旁白、歌词和素材会跟随；删除 Shot 会删除该段 Timeline 实例但保留 Media Pool 原件；跨 Shot 的 Audio/Video 会自动按新顺序拆段并更新 Source In/Out。新增 Shot 是可继续放入素材和 Type Tool 内容的空白 Story Beat。

- **Manual Ripple Cut 保留**：Design 因逐字对白预算而把 45 秒自动延长到 107.5 秒时，Studio 会保留 45 秒为 Storyboard Target。打开 `SMART CUT` 右侧菜单并启用 `MANUAL RIPPLE CUT`，再选择不需要的紫色 Shot Block 按 `Delete`，即可使用原来的人工连锁删除方式；Media Pool 原始素材不会被删除，整个操作支持 Undo/Redo。
- **Timeline-only Visible Text**：字幕与画面文字只能来自可编辑的 Timeline `on_screen_text` Text Layer。`SUB OFF` 或当前生成区间没有文字层时，H3 会收到 Visible Text Lock，禁止自行把对白、旁白、caption 或 lower-third 烧进画面；`SUB ON` 时，允许出现的逐字文字和时间均由 Timeline 白名单决定。
- **剧情标题命名**：`帮我生成45秒视频...总结以下内容如下` 等生成指令不再成为项目文件夹名。系统优先采用有效的 AI 剧情标题；没有标题时跳过命令句，读取第一句真正的剧情内容。

- **Long Timeline Foundation**：一个 Project Timeline 可从 45 秒直接扩展至 120 秒。Smart Render 会保持相同 master seed，把未被编辑的 Approved 0–45 秒锁定复用，只生成新增的 75 秒；如果用户修改旧范围，对应 Segment 才会解除复用并局部重算。Program Monitor、毫秒 Playhead、Render Status、Shot 加权进度、资源估算和最终 Master 均使用完整 Timeline 时长。
- **中断续跑 checkpoint**：每个隐藏 Segment 完成或失败后，Studio 都会自动更新 `project/director_project.h3director.json` 与 `project/render_manifest.json`，不必等完整 Master 输出。重新 Open Project 后，存在且 fingerprint 一致的 Segment 会继续复用。
- **P1 Overview 项目命名**：Media Pool 有图片时，第一张已加载 Picture 的 BLIP `Overview` 是 Workspace 名称第一优先级，例如 `a_young_girl_running_with_a_red_flag`；仅有 imported 素材、尚未保存／生成的 `h3_project` 临时目录会安全改名并同步素材路径。Media Pool 没有图片时，改用 Design Requirement 的第一句有效剧情；不再采用自动生成的 `Create a ... full-reference video` 指令句。

- **P1 唯一人脸来源**：显式要求 `@P1` 保持人脸时，最终 H3 Prompt 会把 `<Subject 1>` 唯一绑定到实际 `<Picture 1>`；引用 P1 权威字眼的 Support 图不再反向夺取身份。由 Z-Image 独立生成、试图用文字重建 P1 人脸的动作图不会装入 H3 请求，旧项目重新 Run 时也会自动过滤这类竞争参考。
- **Workspace 延迟正名**：如果 Design 完成前已建立 `h3_project` 或 `Create_a_...` 临时目录，只要目录仅包含 Manifest、Estimate 与 imported 原始素材，P1 BLIP 完成后即可按 Overview 改名；已保存、已生成及旧项目目录不会被自动移动。
- **Character Continuity Contract**：身份锚点不再只锁脸。默认固定脸、年龄、肤色、发型／发色、身材比例、上下装款式与颜色、裤／裙、鞋子及配饰归属；表情、姿势、手臂／腿部角度、步态以及受风或动作造成的头发／衣物运动可以变化。换装、改变发型、受伤、污渍、破损、脱鞋或配饰转移只能由明确 Shot 触发，并自动继承到后续 Shot，禁止无原因重置。

- **用户 Picture 人脸优先**：当 Requirement 或 Clip Prompt 指定 `@P1` 为全程人脸／身份参考时，`@P1` 会成为唯一权威身份锚点。独立 Z-Image 参考图只能提供环境、道具或动作状态，并避免生成可辨识的替代脸；Ref2VA `summary` 首行保持官方 `[reference generation]` 类型标记。
- **生成参考关键词命名**：若 Qwen 把 `Create a 12-second full-reference video...` 等指令误当项目标题，Workspace 会忽略该标题，并组合第一组生成参考关键词，例如 `marathon_track_morning_sunlight_trees`。
- **可检查 Shot Manifest**：`shots/SHOT_ID/shot_manifest.json` 保存 Shot 时间、状态及 Preview／Final Segment 引用。MP4 仍只保存于 `segments/SEGMENT_ID/takes/`，不会因每个 Shot 重复影片而膨胀。

- **Segment-local unique media**：Smart Render 只全局上传 `unique_media` 联合集；每个 Segment Workflow 保留自己的 Loader 文件名，避免后段素材覆盖前段，适用于虚拟 Media Pool、局部重算、跨 15 秒长片与项目重开。
- **对白时长预算**：Design 按对白语言、字数、标点和 Delivery 估算自然说话时长。没有锁定时间码的过密对白会自动延长所属 Shot，并同步后移后续 Shot、Text Layer、Transition、Marker 和素材范围；用户明确时间码保持不变并显示风险。
- **红色对白风险 Clip**：Dialogue／Voice-over／Lyrics 如果短于安全时长，Timeline Clip 会显示深红底与亮红边，提醒 MiniMax H3 可能提前、换序、漏字或改字。
- **单一人物身份锚点**：三张或以上由 Design 独立生成的人物参考图会选定一个全片主身份锚点；后续图片只负责环境、道具或动作状态，禁止重新定义另一张主角脸。

- **一个项目，一个固定 Workspace**：第一次加入素材、执行 Design 或保存时建立唯一项目目录；后续 Design、Preview、Accept 和 Final 全部留在同一目录，不再为每次操作产生新的主文件夹。
- **非破坏式旧项目迁移**：旧 `.h3director.json` 可以直接打开，原文件不会被修改。Studio 会在其所在目录建立 Workspace 结构，并把后续快照保存到 `project/director_project.h3director.json`。
- **三档成本控制**：`Storyboard` 不运行 H3、`Motion Preview` 使用 0.2MP 且不放大、`Approved Final` 使用已确认 Preview 的同一 seed 在 1.0MP 输出。`ESTIMATE` 会先显示预计处理时间、Shot/Segment 数和新增磁盘占用。
- **Segment Take 与 Shot 引用**：每个实际 Render Segment 最多保存一份 `motion_preview.mp4` 和一份 `approved_final.mp4`；Shot 只记录 Segment、Timeline 范围及 source in/out，不再为共享同一个 15 秒结果的多个 Shot 重复建立 MP4 路径。
- **磁盘保护**：Settings 可调整 `Workspace free disk reserve`，预计生成后低于保留空间时会在开始 TTS／ComfyUI 前停止，不会伤及现有 approved Take。
- **紧凑 Generation Work Area**：主工具栏依次显示 Work Area、Aspect、Mode、Batch、Next、批准秒数、`EXPORT API`、ComfyUI IP、`TEST`、`RUN+QUEUE`、`STORYBOARD`、Story Duration、`AUTO CUT`、`PREVIEW 0.2M`、`ACCEPT 1.0M`、`REJECT` 和 `ESTIMATE`；缩写按钮的完整功能可通过各自的 Tooltip 查看。
- **可编辑对白合约与非阻断修复**：Requirement 要求 Dialogue／Voice-over／旁白／Lyrics 时会先自动重试建立 A-track Text Layer。用户已经提供带时间码的逐字台词时，保护器会恢复原文，真正无法恢复才阻止 Apply；若只是要求“加入合适对白”而 AI 没有给出具体文字，Studio 仍会正常建立 H3 Workspace，并在 Timeline 0 秒处加入红色 `⚠ ADD EDITABLE ...` 提醒。提醒不会进入 H3 Prompt 或 TTS，使用 Type Tool 补上台词即可。
- **字幕明确开关**：Design Requirement 同排新增 `SUB OFF / SUB ON`，默认关闭。关闭时不会生成字幕或 AI 自行发明的 hashtag；开启后从对白层建立同步、可编辑的字幕层。
- **三态原生配乐策略**：Design Requirement 同排提供 `MUSIC AUTO / OFF / TIMELINE`，默认 `AUTO`。AUTO 由 Qwen 分析场景后规划配乐并交给 H3 原生生成；OFF 保证 H3 Prompt 使用 `non_diegetic_music: N/A`；TIMELINE 只服从用户建立的 Music Cue。该状态属于 Project-scoped Render Settings，保存、重开及旧项目迁移均有确定行为。
- **清晰 Master 输出**：Workspace 根目录只发布一份 `generated_preview.mp4` 和一份 `generated_output.mp4`。Segment Takes 保留局部重算能力，成功归档并保存项目后自动清理该次 `.director_cache/generated_outputs` 临时目录。

### 120 秒分批制作操作

1. 在 Special 选择 `long-form-h3-director`，再把完整 120 秒 Requirement 一次交给 Design；不需要自行拆成四份 JSON。
2. Apply 后确认 Timeline 覆盖完整总时长。主页 `MODE` 会自动显示 `BATCH`，默认每批 30 秒。
3. 点击 `NEXT`：Studio 只把下一段（最初为约 0–30 秒）设为 Work Area 并执行 0.2MP Motion Preview。
4. 满意后点击 `ACCEPT 1.0M`：Studio 输出 0 秒至当前批准点的累计 Master，并把批准进度推进至 30 秒；不满意则按 `REJECT` 重做该批 Preview。
5. 重复 `NEXT → ACCEPT` 完成约 30–60、60–90、90–120 秒。若对白跨越 30 秒边界，实际批次会自动延长到整句或安全 Shot 结束。
6. 可以随时 Save／关闭；Open Project 会恢复批准点、待处理批次与 Preview seed。旧范围有编辑时只把相交 Segment 标黄，下一次累计 Final 会重算黄色范围并继续复用其他批准段。

不选择该 Special 时 `MODE` 默认是 `FULL`，`RUN+QUEUE`、`PREVIEW` 与 `ACCEPT` 保持原本一次操作处理当前完整 Work Area 的方式；后台仍可安全拆成不超过 15 秒的原生 H3 Segment。

### 固定 Workspace 结构

```text
Project_Name/
├─ generated_preview.mp4            # 最新 Preview Master（最多一份）
├─ generated_output.mp4             # 最新 Final Master（最多一份）
├─ project/                         # canonical project + render/calibration manifest
├─ design/revisions/R0001.../       # 每次 Design JSON revision
├─ media/imported/                  # 用户载入素材（优先 hard link）
├─ media/generated_references/R.../ # Z-Image／Design 参考图
├─ media/audio/R.../                # Design/TTS 音频
├─ segments/SEGMENT_ID/takes/       # motion_preview + approved_final（各最多一份）
├─ shots/SHOT_ID/shot_manifest.json  # Shot 状态、Segment 引用及 source 时间范围
├─ renders/                         # 内部 render 状态；不再重复完整 Master
├─ proxies/
├─ cache/
└─ logs/
```

旧项目兼容说明：打开 format 18 或更早的 project 时，Studio 只读取原 JSON，并在旁边建立上述目录及 `project_manifest.json`。原 project、照片、声音和视频不会被删除、移动或覆盖；下次保存才会写入新的 canonical project。若把完整 Workspace 搬到另一台电脑，Studio 优先根据“当前打开文件旁边”的结构还原，不会跟随旧电脑遗留的绝对路径。

Alpha.2 已建立 120 秒 Long Timeline 基础；Alpha.3 完成相邻 Shot Dependency Graph 与可检查压缩计划；Alpha.4 的项目级 Batch、OOM 分类重试、Worker checkpoint、Resume 与选择性 Master assembly，以及 Alpha.5 的 Storage Report、Safe Cleanup、Archive 和跨电脑验证，现已合并成为 `v0.3.2` 正式可靠性基线。单一 Studio instance 仍只执行一个 ComfyUI Job；多 Project 可分别保存／恢复及由不同 instance 处理，但“集中式多项目并发调度器”和完整 Scene／Sequence UI 不在本版范围。

- **无限 Virtual Media Pool + Segment 动态装载**：项目可使用 `P10+ / V4+ / A4+`，不再受工作流实体 Loader 总数限制。每个 Segment 只选择自己时间范围内需要的逻辑素材，自动装入 H3 的 9 Image／3 Video／3 Audio 实体插槽，再生成该段专属 `<Picture N> / <Video N> / <Audio N>`。局部重算、重复素材、跨段移动、24 帧 continuity 和项目重载都保持同一永久 `P/V/A` 身份。
- **AI Director Design**：连接 Online GPT 或 LM Studio，把自然语言需求转换成可验证的 Director Design JSON；优先复用 Media Pool 素材，并按缺口调用 Z-Image 生成参考图，再以 BLIP 结果完成第二次视觉校准。
- **可编辑长片生成**：Timeline 保持完整影片视图；超过 15 秒时自动按 Shot／原生窗口拆分、缓存、局部重算并组装 Master，跨段使用上一段最后 24 帧作为无声运动上下文。
- **稳定动态 Mapping**：逻辑素材编号与 ComfyUI 实体 Loader 完全分离；P10 可以在一个 Segment 临时装入第一个实体 Picture Loader，但提示词解码和工程文件中仍永远是 P10，不会误变成 P1。
- **逐字对白与四种声音模式**：支持 MiniMax H3 Native Dialogue、VoxCPM2 Local、Qwen3-TTS Local 和 Edge TTS；Timeline 修改台词、Speaker、时间或 Delivery 后会自动令旧 WAV 失效并重新生成。
- **Design 对白语言锁定**：Design Requirement 同排提供 `LANG` 选择；`Auto` 会从明确语言字眼和需求书写系统确定对白语言。内置 MiniMax H3 稳定支持的 11 种对白语言，错误语言会自动重试并在 Apply 前阻止静默回退英文。
- **完整制作声音**：每个 Shot 自动规划现场对白、连续环境底噪、接触同步 Foley／SFX、受对白自动压低的配乐，以及根据可见空间变化的 Reverb／Echo。
- **项目可恢复**：Preview／Run 自动输出 MP4、Director Project 与长片 manifest 到对应 `example` 工作文件夹；Open Project 可恢复 Timeline、Master、Program Monitor 与 Render Status。中文、乱码、通用标题或被误用作标题的完整指令会改用第一组生成参考关键词，例如 `marathon_track_morning_sunlight_trees`。
