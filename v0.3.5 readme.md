# MiniMax H3 Director Cut Studio v0.3.5-alpha.2

## v0.3.5 重点功能

本版本把音乐与歌声处理拆成两个互不干扰的工作区：

- `MUSIC COVER`：ACE-Step 1.5 的 Cover、Repaint、Lego、Extract、Palette Remix 与精确时长输出。
- `SOULX`：独立 SoulX-Singer 歌声克隆；输入 Voice Reference 与 Source Song，完成后试听并手动保存 MP3，再由使用者把验收文件放入 MTV 的 `A1`。
- `SERVER MODE / CLIENT MODE`：Studio 启动时按当前电脑实际安装状态选择本机服务或局域网服务；Client Mode 不会静默安装大模型。
- 全窗口青色旋转加载遮罩：安装、连接与转换期间锁定操作，避免重复提交。
- SoulX 底部启动状态：逐段显示隔离Runtime、CUDA、模型路径和API合约检测，直到 `SoulX API ready`。
- 关闭本机Server Mode的SoulX窗口时，自动卸载模型并停止7861隐藏进程，完整释放VRAM/DRAM；Client Mode不会关闭共用远端Server。
- Windows SoulX runtime 使用预编译 `webrtcvad-wheels`，避免在项目路径包含空格时被 C 扩展编译失败阻断。
- `MTV Singing H3`：A1 唯一节奏主时钟、5–7秒均衡H3分段、静默陪衬角色、真实歌曲尾段和Accept前 Singing Lip-Sync QC。

部署步骤：

1. 所有电脑先完成 [`Step 1：Studio Runtime`](step1.md)。
2. ACE-Step 主机／副机按 [`Step 2：ACE-Step 1.5`](step2.md) 部署。
3. SoulX 主机／副机按 [`Step 3：SoulX-Singer`](step3.md) 部署。

## Alpha.2：MTV Singing H3 对嘴可靠性

这次更新针对长篇 MTV 在部分时间段突然失去对嘴的问题。Studio 现在把 `A1` 视为整支 MTV 的唯一歌声时间轴；即使 Project 没有人工建立 Lyrics Text Layer，也不会利用 Whisper 从伴奏、人声和环境混合音中猜测歌词。

### 新生成规则

1. 每个 H3 演唱 Segment 自动控制在约 5–7 秒，任何窗口不得超过 7 秒。
2. Segment 在完整工作区内均匀分配，避免最后只剩 1–2 秒的碎片生成。
3. Segment 的本地 `00:00` 必须对应当前 A1 Timeline 位置，不得把歌曲从开头重播。
4. 没有人工歌词时间码时，不生成猜测式开口、闭口、主歌、高潮、尾音或结束嘴形指令。
5. P1 是唯一可辨认的演唱者；P2/P3 只作无声陪衬并保持非发声嘴形。群像镜头仍然只有 P1 可以正面清晰唱歌。
6. 最后一个网格窗口使用 A1 的真实尾音。只有最终时间格比 A1 多出不超过 0.5 秒时，才在尾部补静音；绝不循环、拉伸或重新播放 A1。

### Singing Lip-Sync QC

每个新生成 Segment 下载完成后、进入 Accept 和缓存之前，Studio 会比较：

- H3 未经改写的原生生成音轨；
- 该 Segment 对应的准确 A1 时间窗口；
- 整段节奏、后40%尾段、起音变化与时间偏移。

通过时，相关 Shot 的 `Native Audio QC Status` 显示：

```text
Singing Lip-Sync QC PASS
```

如果节奏漂移、尾段失锁或起音明显错位，Smart Render 不会暂停Job。它会自动调整当前Segment：让P1保持正面或清晰四分之三侧面演唱，嘴唇与下颌无遮挡，并让P2/P3静默、保持非发声嘴形，然后自动重新生成，最多五次。修复期间显示：

```text
Singing Lip-Sync QC AUTO-REPAIR
```

五次仍未达标时显示 `Singing Lip-Sync QC WARNING`，但Job会继续完成，不会停在该段。Warning片段可供本次预览，却不会成为已通过QC的可复用缓存；下次运行该范围仍会重新生成。QC只检测和触发重新调度，不会使用TTS、变声、混响、重混或时间拉伸去修改H3输出。

### 修复已有 MTV Project

旧成片不会被程序直接改写。更新后重新打开 Project：

1. 保持 `MTV Singing H3` Special Skill 与原来的 A1 映射。
2. 对已发现脱嘴的时间范围执行 `Reject`，再运行 `Preview`。
3. 如果多个长段都有问题，建议从 `0.00s` 到歌曲结尾重新生成整支 MTV。
4. 旧版 14 秒缓存会因 Smart Render policy `23` 自动失效；新任务会重新规划为 5–7 秒窗口。
5. 优先接受显示 `Singing Lip-Sync QC PASS` 的片段；如果显示Warning，Job仍会完成，可在预览后决定是否重新运行该范围。

已经保存的旧 MTV Prompt 也会在实际生成前自动清除错误 ASR 文字和猜测式嘴形描述，因此不必重新执行 Design。若用户自行建立准确的 Lyrics Text Layer，其人工文字与时间仍然拥有最高优先级。

### Smart Render H3 常驻与 AIMDO 1450 防护

同一个Smart Render Job中的所有5–7秒Segment会连续使用已经加载的H3模型。Studio不再在每段成功后或Singing QC自动重生前调用ComfyUI `/free`，避免Windows AIMDO反复卸载、重新映射大型权重时出现：

```text
GetOverlappedResult failed error=1450
HostBuffer.read_file_slice failed
```

只有明确的CUDA OOM会触发中途模型清理。`error=1450`、`xfer_file_read*`和`HostBuffer.read_file_slice failed`会被单独识别为 `aimdo_hostbuffer`，重试时保持H3常驻。全部Segment生成并完成Master拼接后，Studio才统一调用一次 `/free`。ComfyUI主机可另外加入 `--disable-pinned-memory`，此启动参数由服务器端管理，不会由Studio静默修改。

## 版本重点：ACE-Step 1.5 Music Workbench

v0.3.5 在 Studio 顶部加入原生 `MUSIC COVER` 工作区，把参考音频理解、音乐条件编辑、Cover/Repaint、分轨生成与分轨提取整合进同一个视窗。`SOULX` 位于 `MUSIC COVER` 左侧，负责独立歌声克隆；两者使用不同 API 与模型。默认局域网服务器为：

```text
http://192.168.0.185:8001
```

工作区支持：

- 上传一段必需的源音频；
- 上传可选的独立音色参考；
- 分析并自动填写 Prompt、Lyrics、BPM、调性、拍号与时长；
- 编辑所有分析结果；
- 调整生成强度、Steps、Seed、输出时长与音量；
- 在没有独立音色参考时，默认同时以源音频控制结构与音色；
- 页面内试听、暂停、停止及下载成品；
- 查看模型初始化及任务的完整 API 返回；
- 以青色旋转加载圈和全视窗半透明遮罩显示长时间任务状态。

## 四种 ACE-Step 模式

| 模式 | 模型 | 用途 | 专用控制 |
|---|---|---|---|
| `Cover` | Turbo 或 Base | 依据源音频重编曲/翻唱 | Cover Strength |
| `Repaint` | Turbo 或 Base | 只重新生成指定时间段 | Start、End、Conservative/Balanced/Aggressive、Repaint Strength |
| `Lego` | Base | 根据完整音频上下文增加指定轨道 | Track、可编辑 Instruction |
| `Extract` | Base | 从完整混音提取指定轨道 | Track、可编辑 Instruction |

Lego/Extract 提供以下轨道：

```text
vocals, backing_vocals, drums, bass, guitar, keyboard,
percussion, strings, synth, fx, brass, woodwinds
```

切换到 Lego 或 Extract 时，Studio 会锁定 `acestep-v15-base` 并把 Steps 范围提高至 Base 模型范围。第一次使用尚未载入的模型时，会在当前生成动作中通过 ACE-Step `/v1/init` 下载/载入，不需要开第二个 API 服务。

## Palette Remix 不再误触发长任务

右侧 Palette 可多选 24 种主要乐器、6 种人声类型、8 个地区/语系类别及 16 种动物声音。

v0.3.5 的执行规则是：

1. 点击 Palette 项目只改变选择和 Prompt，不发送 API 请求。
2. 点击 `REMIX SELECTED PALETTE` 才立即开始生成。
3. 如果忘记点击 Remix 按钮，直接点击当前模式的 `GENERATE`，Studio 会先合并全部尚未提交的 Palette，再开始生成。

因此，即使源音频超过 30 秒，连续挑选乐器或人声也不会重复启动耗时的生成任务。

## 精确输出时长

ACE-Step 的 source-conditioned 任务通常会跟随源音频长度。v0.3.5 会在提交前建立目标长度的结构控制音频，并在下载后再次核对输出：

- 目标短于源音频：精确裁切；
- 目标长于源音频：延展结构控制至目标时长；
- API 返回仍有偏差：在本地完成最终时长校正；
- 调整正音量时加入 peak limiter，降低削波风险。

例如把 20 秒源音频的 Target Duration 改为 120 秒，最终文件会以 120 秒为目标，而不再停留在原曲长度。

## Client Mode 与 Server Mode

所有电脑使用同一份 Studio 和同一个启动入口：

```powershell
.\run_h3_prompt_studio.bat
```

角色不写死在 BAT 内，而是在 Studio 启动时根据当前电脑的真实安装状态决定。

```text
run_h3_prompt_studio.bat
        │
        ▼
Studio 检查本机 ACE-Step runtime + checkpoint/安装标记
        │
        ├─ 完整 ──► SERVER MODE ──► 静默启动 127.0.0.1:8001
        │
        └─ 缺失 ──► CLIENT MODE ──► 不安装，连接 192.168.0.185:8001
```

### Server Mode 判断条件

本机必须同时具备：

- `models/ACE-Step-1.5/.runtime/<电脑名称>/.venv/Scripts/python.exe`，或 ACE-Step 自己的 `.venv/Scripts/python.exe`；
- `models/ACE-Step-1.5/checkpoints/acestep-v15-turbo/config.json`，或由成功安装流程写入的 `.studio_server_mode.json`。

满足条件后，Studio 会调用隐藏启动器。它先检查 `127.0.0.1:8001/health` 和现有 ACE-Step 进程，避免重复启动。

### Client Mode 保证

检测不到完整本机安装时：

- 不执行 Git Clone；
- 不建立 Python/uv 环境；
- 不下载模型；
- 不启动本机端口；
- Music Workbench 默认连接 `http://192.168.0.185:8001`。

只有用户主动点击 `INSTALL LOCAL ACE-STEP`，安装流程才会开始。

## 主机安装 ACE-Step 1.5

以下示例把 `192.168.0.185` 设为推理主机。

### 1. 准备主机

- Windows 10/11；
- NVIDIA 驱动及可正常执行的 `nvidia-smi`；
- 最大单卡 VRAM 严格大于 16GB；
- Git 可用；
- 能够访问 GitHub 与 Hugging Face；
- 完整 Studio 源码及 `ai_libraries_common` runtime。

Studio 使用 `nvidia-smi --query-gpu=memory.total` 检查最大单卡容量。16GB 或更低不会开放本机安装；例如 24GB、32GB 可以。

### 2. 通过 Studio 安装（推荐）

1. 在主机运行：

   ```powershell
   .\run_h3_prompt_studio.bat
   ```

2. 点击顶部的 `MUSIC COVER`（`SOULX` 位于它左侧）。
3. 尚未安装时会显示 `CLIENT MODE`、检测到的显存，以及 `INSTALL LOCAL ACE-STEP`。
4. 点击安装并确认。Studio 会按顺序：

   - Clone 官方 ACE-Step 到 `models/ACE-Step-1.5/`（已有完整源码则跳过）；
   - 安装官方 uv；
   - 建立电脑专属 `.runtime/<电脑名称>/.venv/`；
   - 执行 ACE-Step 依赖同步；
   - 下载 Turbo、VAE/必要模型至 `checkpoints/`；
   - 在后台启动本机 API；
   - 健康检查成功后写入 Server Mode 安装标记。

5. 安装完成后 API 自动改为：

   ```text
   http://127.0.0.1:8001
   ```

6. 测试本机健康状态：

   ```text
   http://127.0.0.1:8001/health
   ```

### 3. 手动安装（诊断用途）

在主机 Studio 根目录执行：

```powershell
git clone https://github.com/ace-step/ACE-Step.git models\ACE-Step-1.5
.\start_ace_step_api.bat
```

如果目录已经存在，不要再次 Clone，直接执行启动器。启动器会使用：

```text
models/ACE-Step-1.5/.runtime/<电脑名称>/.venv/
models/ACE-Step-1.5/checkpoints/
```

Server Mode 的后台日志为：

```text
logs/ace_step_api.stdout.log
logs/ace_step_api.stderr.log
```

### 4. 开放局域网访问

服务器监听 `0.0.0.0:8001`。副机必须能访问：

```text
http://192.168.0.185:8001/health
```

如果本机正常但副机无法连接，请确认：

- 主机 IP 仍然是 `192.168.0.185`；
- Windows 网络类型为 Private；
- Windows Defender Firewall 允许受信任私有网络访问 TCP 8001；
- 路由器/交换器没有启用客户端隔离；
- ACE-Step 命令行没有出现模型或 CUDA 错误。

不要把未配置鉴权的 8001 端口直接映射到公网。

## 副机安装（Client Mode）

副机只安装普通 Studio Runtime：

1. 放置完整 Studio 源码。
2. 解压 `ai_libraries_common/`。
3. 按主 README 安装需要的 Qwen3-TTS/其他 Studio 组件。
4. 运行：

   ```powershell
   .\run_h3_prompt_studio.bat
   ```

5. Studio 检测不到完整本机 ACE-Step 后进入 `CLIENT MODE`。这一步不会安装 ACE-Step。
6. 打开 `MUSIC COVER`，API 应显示：

   ```text
   http://192.168.0.185:8001
   ```

7. 点击 `TEST API`。成功后即可上传副机音频，让主机完成 Analysis、Cover、Repaint、Lego 或 Extract；生成结果会下载回副机的 Studio 输出目录。

如果副机自身也有大于 16GB 的 NVIDIA GPU，可以选择主动安装并把它升级成 Server Mode；不点击安装按钮则始终保持 Client Mode。

## 日常启动顺序

### 主机

```text
双击 run_h3_prompt_studio.bat
→ Studio 判断 Server Mode
→ 后台自动启动 ACE-Step
→ Music Workbench 使用 127.0.0.1:8001
```

### 副机

```text
双击 run_h3_prompt_studio.bat
→ Studio 判断 Client Mode
→ 不安装/不启动 ACE-Step
→ Music Workbench 使用 192.168.0.185:8001
```

主机必须保持开机并运行 Studio/ACE-Step。副机无法通过一个已经关闭的 HTTP API 反向启动已关机的主机。

## 兼容性

- 应用版本：`0.3.5-alpha.2`
- Director Project 格式：`25`
- 旧 `.h3director.json` 项目无需迁移；Music Workbench 与 Client/Server Mode 不改变项目保存结构。
- 原有 H3 Ref2VA、Design、Timeline、Smart Render、TTS、4DX 与 Special Skills 保持兼容。
