# MiniMax H3 Director Cut Studio v0.3.3

## v0.3.3-alpha.1 · AI Movie Making-of / VFX Breakdown / 4DX Scene Composer

这一版新增的不是普通 4DX 特效开关，而是一套完整影片格式 Special Skill：`ai-movie-making-of-4dx`。根据首支验收片 `h3_project_291` 的结果，格式已进一步修正为真正的「10种4DX电影效果 Making-of」示范。

它会按效果数量自动决定最低时长。默认完整影片包含：开场、Pitch、Roll、Heave、Wind、Air Shot、Vibration、Impact、Flash、Fog/Smoke、Water/Rain，以及稳定收尾。每一种效果都有自己的电影场景和Making-of因果链，不再使用旧版的素材分析、人物提取、环境重建与合成九阶段作为影片主线。

时长公式是：第一种效果15秒，每增加一种再增加5秒。即1种=15秒、2种=20秒、3种=25秒，直到10种=60秒。Design Requirement写得更长会保留；写得更短会自动提高至规格下限。`4DX EXPERIENCE CONTROL`和Design页面的Duration状态会同步显示用户目标、效果数量、最低时长与最终时长。

根据 `a_woman_with_long_black_hair_and_a_white_top_11` 的实片验收，格式已进一步改成“现场体验优先”：开场和收尾各0.5秒，其余章节至少99%的时间同时呈现银幕事件、4DX影厅装置、互动灯光、现场声音和观众反馈。自动旁白已经取消。

根据 `a_woman_with_long_black_hair_and_a_white_top_12` 的实片验收，新增 `Screen-Plane Breakout`：影片事件不能只被困在矩形银幕内。每章只让一个效果载体越过银幕平面并短暂遮挡银幕边框，再把同一方向的风、光、水、烟、碎片或压力延伸至前排上方。前排先按来向闪避，中排稍迟后仰，后排再以较小幅度惊呼或指向。主体始终只有一个，禁止复制飞机、车辆或人物，也禁止以传送门、独立全息影像或第二个完整主体冒充4D突出效果。

根据 `a_woman_with_long_black_hair_and_a_white_top_13` 的音频验收，修正了两个实际编译漏洞：主页的 `MUSIC AUTO` 不再覆盖本Skill的 `MUSIC OFF`；Native Audio也不会再把4DX影厅误判成办公室。每章现在强制同时生成银幕事件、座椅／机械Foley、定向环境装置及前中后排观众反应四层H3原生现场声，物理效果和观众声音优先于安静的影厅底噪。

### 使用方法

1. 在主页 Special Skill 选择 `Ai Movie Making Of 4dx`。
2. 点击 `DESIGN`。Design Requirement 会自动载入完整格式模板。
3. `4DX EXPERIENCE · SELECT ONE OR MORE` 默认已经选择全部十项：Pitch、Roll、Heave、Wind、Air Shot、Vibration、Impact、Flash、Fog/Smoke、Water/Rain。你也可以取消不需要的章节。
4. 分别选择 `INTENSITY` 与 `DENSITY`。这些设定会自动更新 Design Requirement 内由 Studio 管理的 4DX 区块，不会覆盖你写的故事。
5. 在 Media Pool 启用需要使用的 P/V/A 素材，并在需要强制绑定时写明 `@P1`、`@V1` 或 `@A1`。
6. 点击 `CREATE DIRECTOR DESIGN JSON`。检查 JSON 与 Preflight 后点击 `APPLY TO H3 WORKSPACE`。
7. 建议按 Batch／Incremental 分段运行 Preview，确认每段后再 Accept。

### 素材与自动生成原则

- P1-P9 没有固定角色。Studio 根据用户映射、锁定、原始素材、AI Enrich 和 BLIP/VAD/Whisper 建立 Reference Role Ledger。
- 文件名不是画面证据，不能把旧项目名称、地标名称或角色名称带入新视频。
- 优先复用已经加载而且内容兼容的素材。每个被选择的效果章节最多申请一张time-scoped Z-Image场景图，不会继续扩张成每Shot多图、替代版本或P70。
- Technical label、Before/After、箭头和参数属于可编辑 Timeline 文字／图形层；H3 负责干净的电影画面。

### 4DX 因果原则

每个被选择的效果都会建立独立章节。章节先展示可辨认的实拍装置或片场准备，再match cut至原创电影成片；画面必须先出现物理原因，随后才出现受力、材质反应、衰减和4DX反馈。系统分别保存：

- `physical_events`：画面中的来源、目标、接触／擦身、方向、强度、持续时间与材质反馈；
- `fourdx_events`：由 Physical Event 映射得到的体验类型、强度与密度。

两者使用稳定 ID 连接。摄影机 Pan、Tilt、Dolly、Orbit、Zoom 或 Shake 不会被误当成座椅运动。这个Showcase会主动为每个已选择章节设计兼容的可见原因，因此默认十项都会生成一组Physical Event与4DX Event；取消选择的项目才不会建立章节和事件。

### 默认十章示范内容

| 章节 | 原创电影场景 | 主要4DX逻辑 |
| --- | --- | --- |
| Pitch | 战机甲板起飞与爬升 | 加速／俯冲的前后倾斜 |
| Roll | 湿地高速过弯 | 横向惯性与回正 |
| Heave | 救援舱坠落与着陆 | 失重、下降与承托 |
| Wind | 龙卷风追逐 | 持续定向风与衰减 |
| Air Shot | 高速碎片擦身 | 单次定向喷气 |
| Vibration | 地下机械逼近 | 低频震动包络 |
| Impact | 车辆撞击障碍 | 接触后的单次冲击 |
| Flash | 天台雷击 | 接触同步闪光 |
| Fog / Smoke | 火场开门释放烟层 | 烟雾扩散与消散 |
| Water / Rain | 救援船撞上风浪 | 瞬时喷水与持续降雨 |

### 音频边界

这个Skill固定使用 `MUSIC OFF`，即使主页保持 `MUSIC AUTO`，实际编译给H3时也会强制关闭配乐；不建立旁白、对白、主持人或歌词层。每章同时保留四层H3原生现场声：前方银幕事件、近距离座椅／机械Foley、与画面方向同步的环境装置，以及按距离延迟的观众喘息、惊呼、短叫、闪避声与笑声。不会加入TTS替换、语音分离、FFmpeg混响、EQ、卷积混响或后期重混。

### 现场观众与沉浸式运镜

- 每章必须看见专业4DX电影院、大银幕、三排或以上运动座椅和多个不同成年观众。
- 观众根据当前效果作出不同反应；禁止全员静止、复制脸孔或同步做相同动作。
- 银幕方向、座椅受力、风水烟雾、互动灯光、声音与身体反馈必须共享触发时间和衰减。
- 每章必须保留可辨认的银幕边框，并让一个效果元素遮住一小段边框后进入前排上方；这是判断“突出银幕”而不是普通播放的深度证据。
- 观众反应按空间传播：前排立即定向闪避，中排稍迟后仰，后排较小幅度惊呼、指向或互相确认。
- FPV摄影机沿通道真实移动，并围绕中央观众群作宽阔空间弧线；禁止出现无人机、原地360度旋转、桶滚和Zoom Out。
- 场地不得变成宴会厅、会议室、电脑室、办公室或电竞模拟器。

完整验收项目见 [`V0.3.3-ALPHA.1_AI_MOVIE_MAKING_OF_4DX_ACCEPTANCE_CHECKLIST.md`](V0.3.3-ALPHA.1_AI_MOVIE_MAKING_OF_4DX_ACCEPTANCE_CHECKLIST.md)。
